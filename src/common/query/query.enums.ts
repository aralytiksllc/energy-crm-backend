export enum Operator {
  EQ = 'eq',
  NE = 'ne',
  GT = 'gt',
  GTE = 'gte',
  LT = 'lt',
  LTE = 'lte',
  LIKE = 'like',
  ILIKE = 'ilike',
  IN = 'in',
  RANGE = 'range',
}

export enum Sort {
  ASC = 'ASC',
  DESC = 'DESC',
}
